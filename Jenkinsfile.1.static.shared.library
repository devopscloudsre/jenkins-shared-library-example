@Library("static-shared-lib@master") _
node {
    helloWorld "Hello from staticly configured shared library"

    // @Library("static-shared-lib@master") 
    // import by.mailrahulsre.jenkins.lib.HelloWorld
}